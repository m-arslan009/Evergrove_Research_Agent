# Environments and secrets
.venv/
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local data: SQLite memory, cache, traces (plan 12)
data/
*.sqlite3
*.sqlite3-journal

# Local scratch. Named one by one, never a pattern: `fixtures/` is a documented set with a
# provenance policy (fixtures/README.md), and a wildcard here would silently hide a real one.
fixtures/documents/Sample.txt

# OS
Thumbs.db
.DS_Store
